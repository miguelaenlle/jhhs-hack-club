import * as React from "react"
const WorkshopItem: React.FC<{
    workshopNumber: number;
    title: string;
    description: string;
    startDate: string;
    link: string;
}> = (props) => {
    return (
        <div className={"bg-gradient-to-b from-neutral-800 to-neutral-900 p-10"}>

        </div>
    );
}
export default WorkshopItem